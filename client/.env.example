# Client Configuration (if needed)
# Most client configuration is handled by Vite automatically

# API Base URL (auto-configured in development via Vite proxy)
# VITE_API_BASE_URL=http://localhost:3001/api

# Environment
NODE_ENV=development
